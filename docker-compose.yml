version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - PORT=8000
    volumes:
      - ./uploads:/app/uploads
      - ./.llm_cache:/app/.llm_cache
